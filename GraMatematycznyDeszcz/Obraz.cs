using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace GraMatematycznyDeszcz
{
    class Obraz//:IDisposable
    {
       /* bool disposed = false;

        Bitmap bitmap;
        private int x;
        private int y;

        public int obrazX { get { return x; } set { x = value; } }
        public int obrazY { get { return y; } set { y = value; } }

        public Obraz(Bitmap bitmap)
        {
            bitmap = new Bitmap(bitmap);
        }

        public void DrawImage(Graphics g)
        {
            g.DrawImage(bitmap, x, y);
        }

        public void Dispose()
        {
            Dispose(true);
            GC.SuppressFinalize(this);
        }

        protected virtual void Dispose(bool disposing)
        {
            if (disposed)
                return;

            if (disposing)
            {
                bitmap.Dispose();
            }

            disposed = true;
        }*/
    }
}
